Factory_Methord :

구조와 구현을 분리해서 사용하는 패턴. 템플릿 메소드와 매우 흡사하다.

아이템(대상)과 생성기가 쌍으로 있는 듯하다.

만약, 외부 라이브러리에 아이템과 아이템 생성 추상 클래스가 정의되어 있다면
실제 구현부는 아이템 구현부와 생성기 구현부로 나눠 만들면 된다.

예시)
아이템은 use()라는 함수가 존재한다. 생성기는 아이템 이름을 받아서 아이템을 생성해준다.
(추가로 아이템 생성기의 단계는 DB - 로그 - 생성 로 나눈다고 가정한다.)